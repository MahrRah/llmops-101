# Sample - Prompt flow

## Run

Set up like all the other samples with a predefined chat history. Run the following command to achieve the equivalent results as for the other small samples:

```sh
pf flow test --flow sample:chat --inputs question="Do other Azure AI services support this too?"
```
